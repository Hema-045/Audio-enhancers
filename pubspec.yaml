name: ai_hearing_assistant
description: AI Hearing Assistant MVP (Flutter + native Android)
publish_to: none
version: 0.1.0
environment:
  sdk: ">=3.0.0 <4.0.0"
dependency_overrides:
  meta: ^1.18.3
  vector_math: ^2.4.0
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  permission_handler: ^12.0.3
  flutter_foreground_task: ^9.2.2

flutter:
  uses-material-design: true
